# Leave unset in local dev — Vite proxies `/api` to the backend.
# For split deploys or staging builds, point the SPA at your API origin (no trailing slash).
# VITE_API_BASE=https://api.example.imarah.my
