{
  "name": "imarah-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["web", "backend"],
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"npm run dev -w web\" \"npm run dev -w backend\"",
    "build": "npm run build -w backend && npm run build -w web",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:migrate": "npm run prisma:migrate -w backend",
    "db:seed": "npm run prisma:seed -w backend",
    "lint": "npm run lint -w web && npm run lint -w backend",
    "format": "prettier -w \"**/*.{ts,tsx,js,json,md}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "prettier": "^3.6.2"
  }
}
